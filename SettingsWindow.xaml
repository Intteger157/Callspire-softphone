<Window x:Class="Softphone.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="700"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">
    
    <Border Background="{StaticResource BackgroundDarkBrush}" 
            CornerRadius="8" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Settings" 
                          FontSize="14" FontWeight="SemiBold" 
                          Margin="16,10,0,10" 
                          VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" Width="46" Height="32" 
                       Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                       BorderThickness="0" Click="CloseButton_Click"
                       FontSize="16" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentRedBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource BackgroundMediumBrush}" 
                       BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Settings" 
                                  FontSize="18" FontWeight="Bold" 
                                  Margin="16,16,16,24"/>

                        <StackPanel Grid.Row="1" Margin="8,0,8,0">
                            <Button Content="Connection" 
                                   Style="{StaticResource SidebarButtonStyle}" 
                                   Click="ConnectionButton_Click" 
                                   Name="ConnectionButton"/>
                            <Button Content="Audio" 
                                   Style="{StaticResource SidebarButtonStyle}" 
                                   Click="AudioButton_Click" 
                                   Name="AudioButton"/>
                            <Button Content="General" 
                                   Style="{StaticResource SidebarButtonStyle}" 
                                   Click="GeneralButton_Click" 
                                   Name="GeneralButton"/>
                        </StackPanel>

                        <Button Grid.Row="2" Content="Close" 
                               Style="{StaticResource SidebarButtonStyle}" 
                               Click="CloseButton_Click" 
                               Margin="8,0,8,8"/>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Column="1" Background="{StaticResource BackgroundDarkBrush}">
                    <!-- Connection Settings View -->
                    <Grid Name="ConnectionSettingsView" Visibility="Visible" Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Connection Settings" 
                                  FontSize="20" FontWeight="Bold" 
                                  Margin="0,0,0,24"/>

                        <TextBlock Grid.Row="1" Text="SIP Server:" 
                                  Margin="0,0,0,8"/>
                        <TextBox Grid.Row="2" Name="SipServerTextBox" 
                                Style="{StaticResource ModernTextBoxStyle}" 
                                Margin="0,0,0,16"/>

                        <TextBlock Grid.Row="3" Text="SIP Username:" 
                                  Margin="0,0,0,8"/>
                        <TextBox Grid.Row="4" Name="SipUsernameTextBox" 
                                Style="{StaticResource ModernTextBoxStyle}" 
                                Margin="0,0,0,16"/>

                        <TextBlock Grid.Row="5" Text="SIP Password:" 
                                  Margin="0,0,0,8"/>
                        <PasswordBox Grid.Row="6" Name="SipPasswordBox" 
                                    Width="300" 
                                    Background="{StaticResource BackgroundLightBrush}"
                                    Foreground="{StaticResource TextPrimaryBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    Padding="12,8"
                                    FontSize="14"
                                    FontFamily="Segoe UI"
                                    Margin="0,0,0,24"/>

                        <Button Grid.Row="7" Content="Save" 
                               Background="{StaticResource AccentBlueBrush}"
                               Foreground="White"
                               BorderThickness="0"
                               Padding="24,10"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Cursor="Hand"
                               Click="SaveConnectionSettings_Click"
                               HorizontalAlignment="Left">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{StaticResource AccentBlueBrush}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#4752C4"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                    <!-- Audio Settings View -->
                    <Grid Name="AudioSettingsView" Visibility="Collapsed" Margin="24">
                        <TextBlock Text="Audio settings — coming soon" 
                                  FontSize="16" 
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                    </Grid>

                    <!-- General Settings View -->
                    <Grid Name="GeneralSettingsView" Visibility="Collapsed" Margin="24">
                        <TextBlock Text="General settings — coming soon" 
                                  FontSize="16" 
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

