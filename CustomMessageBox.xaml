<Window x:Class="Softphone.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
        Title="Message" 
        SizeToContent="WidthAndHeight"
        MinHeight="200"
        MinWidth="400"
        MaxHeight="800"
        MaxWidth="900"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <!-- WindowChrome не нужен для AllowsTransparency="True" -->
    
    <!-- Theme dictionaries are provided by App.xaml (System/Dark/Light). -->
    
    <!-- Фоновый Grid заполняет все окно полностью, чтобы скрыть серые углы -->
    <!-- Тень убрана, чтобы избежать артефактов в углах -->
    <Grid Background="{DynamicResource DialogBackgroundBrush}" Margin="0">
        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border x:Name="WindowSurface"
                    Background="{DynamicResource DialogBackgroundBrush}" 
                    CornerRadius="6" 
                    ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title Bar -->
                    <Border x:Name="TitleBarBorder"
                            Grid.Row="0"
                            Background="{DynamicResource DialogChromeBrush}" 
                            CornerRadius="6,6,0,0"
                            ClipToBounds="True">
                        <Grid MouseDown="TitleBar_MouseDown">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                       Name="TitleTextBlock"
                                       Text="Message" 
                                       Foreground="{DynamicResource TextPrimaryBrush}" 
                                       FontSize="16" 
                                       FontWeight="SemiBold" 
                                       VerticalAlignment="Center"
                                       Margin="16,12,0,12"/>
                            
                            <Button Grid.Column="1"
                                    Name="CloseButton"
                                    Content="✕"
                                    Click="CloseButton_Click"
                                    Style="{DynamicResource TitleBarCloseButtonStyle}"
                                    Height="36"/>
                        </Grid>
                    </Border>

                    <!-- Content -->
                    <Grid Grid.Row="1" Margin="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" MinWidth="300"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon (do not depend on system icon fonts; Win10 can miss Segoe Fluent Icons) -->
                        <ic:SymbolIcon Grid.Column="0"
                                       Name="IconSymbol"
                                       Symbol="Info"
                                       Width="32"
                                       Height="32"
                                       IconVariant="Regular"
                                       Foreground="{DynamicResource AccentBlueBrush}"
                                       VerticalAlignment="Top"
                                       Margin="0,4,16,0"/>
                        
                        <!-- Message with ScrollViewer -->
                        <ScrollViewer Grid.Column="1" 
                                      VerticalScrollBarVisibility="Hidden"
                                      HorizontalScrollBarVisibility="Disabled"
                                      PanningMode="VerticalOnly"
                                      Padding="0"
                                      Margin="0"
                                      MaxHeight="600">
                            <TextBlock Name="MessageTextBlock"
                                       Text="Message text" 
                                       Foreground="{DynamicResource TextPrimaryBrush}" 
                                       FontSize="14"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Top"
                                       MinWidth="300"/>
                        </ScrollViewer>
                    </Grid>

                    <!-- Buttons -->
                    <Border Grid.Row="2" 
                            Background="{DynamicResource DialogChromeBrush}" 
                            CornerRadius="0,0,6,6" 
                            ClipToBounds="True">
                        <Grid Margin="16,12">
                            <Button Name="OkButton"
                                    Content="OK" 
                                    Width="80"
                                    Height="32"
                                    Background="{DynamicResource AccentBlueBrush}"
                                    Foreground="White"
                                    BorderThickness="0"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Cursor="Hand"
                                    Click="OkButton_Click"
                                    HorizontalAlignment="Right">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="4"
                                                            SnapsToDevicePixels="True">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                          VerticalAlignment="Center" 
                                                                          Margin="16,8"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#4752C4"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="#3A45C1"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Border stroke overlay (no layout inset) -->
            <Border x:Name="WindowStroke"
                    Background="Transparent"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    IsHitTestVisible="False"/>
        </Grid>
    </Grid>
</Window>
